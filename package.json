{
  "name": "mbalmer-datastore",
  "version": "0.1.0",
  "description": "Datastore",
  "scripts": {
    "start": "concurrently \"npm run compile:watch\" \"npm run test:watch\"",
    "compile:watch": "tsc -w",
    "test": "mocha -R spec tests/*.test.js",
    "test:watch": "npm test -- --watch"
  },
  "dependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.2",
    "@types/sinon": "^5.0.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "sinon": "^6.0.0",
    "typescript": "^2.9.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
